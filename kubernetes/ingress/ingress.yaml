apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: kubecommerce-ingress
spec:
  ingressClassName: traefik
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: gateway
                port:
                  number: 8000
